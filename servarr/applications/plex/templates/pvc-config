apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-config
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: truenas-iscsi-nonroot
  volumeName: pv-plex-config
  volumeMode: Block
  resources:
    requests:
      storage: 100Gi