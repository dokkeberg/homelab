apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-tv
spec:
  capacity:
    storage: 1Ti
  accessModes:
    - ReadOnlyMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: /mnt/hdd1/multimedia/tv
    server: {{ .Values.nas.host }}
  storageClassName: nfs-multimedia-tv