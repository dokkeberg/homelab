apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: seerr-data-pv
  resources:
    requests:
      storage: 25Gi
